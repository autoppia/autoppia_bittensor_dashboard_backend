# FastAPI and ASGI server
# Bittensor pins FastAPI to ~=0.110.1, so keep us on the same minor line.
fastapi>=0.110.1,<0.111
uvicorn[standard]>=0.30.0

# Data validation and settings
pydantic==2.8.0
pydantic-settings>=2.4.0

# SQL database support
SQLAlchemy>=2.0.30
asyncpg>=0.29.0

# Environment management
python-dotenv>=1.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
httpx>=0.27.0

# AWS integration
boto3>=1.35.0
botocore>=1.35.0

# Validator auth dependency (kept optional at install time but required in prod)
bittensor>=6.9.0,<10.0.0

# Form data support
python-multipart>=0.0.6