# FastAPI and ASGI server (0.116+ for Starlette 0.47+ CVE fixes)
fastapi>=0.116.0,<0.117
uvicorn[standard]>=0.30.0
starlette>=0.47.2

# Data validation and settings
pydantic==2.8.0
pydantic-settings>=2.4.0

# SQL database support
SQLAlchemy>=2.0.30
asyncpg>=0.29.0

# Redis cache support
redis>=5.0.0

# Environment management
python-dotenv>=1.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
httpx>=0.27.0

# AWS integration
boto3>=1.35.0
botocore>=1.35.0

# Validator auth dependency (kept optional at install time but required in prod)
bittensor>=6.9.0,<10.0.0

# Form data support
python-multipart>=0.0.6

# Async file I/O (e.g. export scripts)
aiofiles>=24.1.0

# Pinned for CVE fixes (pip-audit)
setuptools>=78.1.1
